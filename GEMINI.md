# Gemini Project Configuration

This file provides context and instructions for the Gemini agent to work effectively on this project.

## Project Overview

This project is a React application built with TypeScript. The main application entry point is `src/App.tsx`. It appears to be a game, given the file names like `Enemy.tsx`, `Player.tsx`, `useGameLoop.ts`, and various sound/image assets.

## Instructions for Gemini

- **Style Guide:** Please follow the existing coding style.
- **Dependencies:** Use `npm` to manage dependencies. Check `package.json` for existing libraries before adding new ones.
- **Testing:** (Please specify testing instructions here, e.g., "Use `npm test` to run tests.")
- **Deployment:** (Please specify deployment instructions here, e.g., "Use `npm run build` to create a production build.")

## Common Tasks

- To run the app locally, use `npm start`.
- To build the app, use `npm run build`.
